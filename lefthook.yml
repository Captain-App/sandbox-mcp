# lefthook.yml - Git hooks configuration

pre-commit:
  parallel: true
  jobs:
    - name: format
      run: npm run format:check
    - name: lint
      run: npm run lint

pre-push:
  jobs:
    - name: check
      run: npm run check
